openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Workforce OS v2 API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: artifacts
    description: Outreach artifact operations
  - name: activity
    description: Agent activity stream
  - name: today
    description: Today page data
  - name: leads
    description: Lead and pipeline management
  - name: conversations
    description: Inbox and conversation threads
  - name: settings
    description: Organization settings and compliance
  - name: runs
    description: Pipeline run history
  - name: welcome
    description: Onboarding wizard
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /artifacts/pending:
    get:
      operationId: listPendingArtifacts
      tags: [artifacts]
      summary: List pending review artifacts
      parameters:
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 5 }
      responses:
        "200":
          description: Paginated list of pending artifacts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedArtifacts"

  /artifacts:
    get:
      operationId: listArtifacts
      tags: [artifacts]
      summary: List all artifacts with optional status filter
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              [
                DRAFT,
                PENDING_REVIEW,
                APPROVED,
                REJECTED,
                SENT,
                SUPPRESSED,
                SENDING,
                SIMULATED,
                DELIVERY_UNKNOWN,
                FAILED,
              ]
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
      responses:
        "200":
          description: List of outreach artifacts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedArtifacts"

  /artifacts/{id}:
    get:
      operationId: getArtifact
      tags: [artifacts]
      summary: Get a single artifact by ID
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Artifact detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutreachArtifact"
        "404":
          description: Not found

  /artifacts/{id}/approve:
    post:
      operationId: approveArtifact
      tags: [artifacts]
      summary: Approve an outreach artifact
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Approved artifact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutreachArtifact"
        "503":
          description: Approval may be durably saved even though queue handoff failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactApprovalFailure"
        "404":
          description: Not found

  /artifacts/{id}/reject:
    post:
      operationId: rejectArtifact
      tags: [artifacts]
      summary: Reject an outreach artifact
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectInput"
      responses:
        "200":
          description: Rejected artifact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutreachArtifact"

  /artifacts/{id}/suppress:
    post:
      operationId: suppressArtifact
      tags: [artifacts]
      summary: Suppress recipient and artifact
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Recipient suppressed; artifact status changes only when it is safe to do so
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactSuppressionResult"

  /activity:
    get:
      operationId: getActivityStream
      tags: [activity]
      summary: Get recorded activity stream
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
        - name: filter
          in: query
          schema:
            type: string
            enum: [all, outbound, pipeline]
            default: all
      responses:
        "200":
          description: Activity events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityEvent"

  /today/kpis:
    get:
      operationId: getTodayKpis
      tags: [today]
      summary: Get today's KPI tiles
      responses:
        "200":
          description: KPI data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TodayKpis"

  /leads:
    get:
      operationId: listLeads
      tags: [leads]
      summary: List leads with filters
      parameters:
        - name: minScore
          in: query
          schema: { type: integer }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: q
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Paginated leads
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedLeads"

  /leads/{id}:
    get:
      operationId: getLead
      tags: [leads]
      summary: Get lead detail
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Lead detail with evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadDetail"
        "404":
          description: Not found

  /leads/bulk-suppress:
    post:
      operationId: bulkSuppressLeads
      tags: [leads]
      summary: Suppress multiple leads
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkLeadIds"
      responses:
        "200":
          description: Per-person suppression results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkPersonSuppressionResult"

  /conversations:
    get:
      operationId: listConversations
      tags: [conversations]
      summary: List conversation threads
      parameters:
        - name: sentiment
          in: query
          schema:
            type: string
            enum: [positive, objection, neutral, negative]
        - name: unread
          in: query
          schema: { type: boolean }
        - name: needsReply
          in: query
          schema: { type: boolean }
        - name: archived
          in: query
          schema: { type: boolean }
        - name: leadId
          in: query
          schema: { type: string }
        - name: search
          in: query
          description: Case-insensitive search across contact name, contact email, subject, and latest-message preview
          schema: { type: string, minLength: 1, maxLength: 200 }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
      responses:
        "200":
          description: Conversation list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedConversations"

  /conversations/{id}:
    get:
      operationId: getConversation
      tags: [conversations]
      summary: Get full conversation thread
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Full conversation with messages and reply intelligence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDetail"
        "404":
          description: Not found

  /conversations/{id}/draft-reply:
    post:
      operationId: draftReply
      tags: [conversations]
      summary: Generate a reply draft inline
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Existing active, sent, or delivery-unknown reply artifact returned idempotently
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerResult"
        "202":
          description: New reply artifact created and held for review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerResult"

  /conversations/{id}/read:
    post:
      operationId: markConversationRead
      tags: [conversations]
      summary: Mark a conversation as read
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Conversation marked read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkActionResult"

  /conversations/{id}/replies:
    post:
      operationId: createConversationReply
      tags: [conversations]
      summary: Resolve a human-written reply artifact for review
      description: Creates a PENDING_REVIEW artifact or returns the existing artifact for the same inbound message. This endpoint never sends the reply.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationReplyInput"
      responses:
        "200":
          description: Existing active, sent, or delivery-unknown reply artifact returned idempotently
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerResult"
        "201":
          description: Reply artifact created and held for review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerResult"

  /conversations/{id}/follow-ups:
    post:
      operationId: createConversationFollowUp
      tags: [conversations]
      summary: Create an internal follow-up reminder
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationFollowUpInput"
      responses:
        "201":
          description: Follow-up reminder created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationFollowUp"

  /conversations/{id}/follow-ups/{followUpId}:
    patch:
      operationId: updateConversationFollowUp
      tags: [conversations]
      summary: Complete or cancel an open follow-up reminder
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: followUpId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateConversationFollowUpInput"
      responses:
        "200":
          description: Follow-up reminder updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationFollowUp"

  /conversations/{id}/meetings:
    post:
      operationId: createConversationMeeting
      tags: [conversations]
      summary: Record an internal meeting proposal
      description: Records a proposal in the meeting ledger. This does not send an invite or contact the lead.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationMeetingInput"
      responses:
        "201":
          description: Internal meeting proposal recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /meetings/{meetingId}:
    patch:
      operationId: updateConversationMeeting
      tags: [conversations]
      summary: Edit a proposed or confirmed meeting ledger entry
      parameters:
        - name: meetingId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateConversationMeetingInput"
      responses:
        "200":
          description: Meeting ledger entry updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /meetings/{meetingId}/confirm:
    post:
      operationId: confirmConversationMeeting
      tags: [conversations]
      summary: Confirm a proposed meeting
      parameters:
        - name: meetingId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Meeting confirmed or already confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /meetings/{meetingId}/cancel:
    post:
      operationId: cancelConversationMeeting
      tags: [conversations]
      summary: Cancel a proposed or confirmed meeting
      parameters:
        - name: meetingId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelConversationMeetingInput"
      responses:
        "200":
          description: Meeting cancelled or already cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /meetings/{meetingId}/complete:
    post:
      operationId: completeConversationMeeting
      tags: [conversations]
      summary: Mark a confirmed meeting completed
      parameters:
        - name: meetingId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Meeting completed or already completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /meetings/{meetingId}/no-show:
    post:
      operationId: markConversationMeetingNoShow
      tags: [conversations]
      summary: Mark a confirmed meeting no-show
      parameters:
        - name: meetingId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Meeting marked no-show or already no-show
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMeeting"

  /conversations/{id}/archive:
    post:
      operationId: archiveConversation
      tags: [conversations]
      summary: Archive a conversation thread
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkActionResult"

  /conversations/{id}/unarchive:
    post:
      operationId: unarchiveConversation
      tags: [conversations]
      summary: Restore an archived conversation thread
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Restored or already active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkActionResult"

  /runs:
    get:
      operationId: listRuns
      tags: [runs]
      summary: Paginated GraphRun history
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, maximum: 10000, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: status
          in: query
          schema:
            type: string
            enum: [RUNNING, AWAITING_APPROVAL, COMPLETED, FAILED, CANCELLED]
      responses:
        "200":
          description: Paginated graph runs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedRuns"

  /runs/trigger:
    post:
      operationId: triggerRun
      tags: [runs]
      summary: Start a new pipeline run
      responses:
        "202":
          description: Run started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerResult"

  /runs/{id}:
    get:
      operationId: getRun
      tags: [runs]
      summary: Get one run and its persisted timeline
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Run with its tenant-scoped persisted timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphRunDetail"
        "404":
          description: Not found

  /settings/org:
    get:
      operationId: getOrgSettings
      tags: [settings]
      summary: Get organization settings and compliance state
      responses:
        "200":
          description: Organization settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgSettings"
    put:
      operationId: updateOrgSettings
      tags: [settings]
      summary: Update organization settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrgInput"
      responses:
        "200":
          description: Updated org settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgSettings"

  /settings/org/health:
    get:
      operationId: getOrgHealth
      tags: [settings]
      summary: Get org compliance health summary
      responses:
        "200":
          description: Org health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgHealth"

  /settings/suppressions:
    get:
      operationId: listSuppressions
      tags: [settings]
      summary: List the authoritative outreach suppression registry
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
        - name: cursor
          in: query
          schema: { type: string, minLength: 1 }
      responses:
        "200":
          description: One cursor page of suppression rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuppressionPage"
    post:
      operationId: createSuppression
      tags: [settings]
      summary: Record an operator-observed opt-out or complaint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSuppressionInput"
      responses:
        "200":
          description: Suppression already existed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSuppressionResult"
        "201":
          description: Suppression recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSuppressionResult"

  /settings/icp:
    get:
      operationId: getIcpProfile
      tags: [settings]
      summary: Get ICP profile
      responses:
        "200":
          description: ICP profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IcpProfile"
    put:
      operationId: updateIcpProfile
      tags: [settings]
      summary: Update ICP profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IcpProfile"
      responses:
        "200":
          description: Updated ICP profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IcpProfile"

  /settings/integrations:
    get:
      operationId: listIntegrations
      tags: [settings]
      summary: List integration connection states
      responses:
        "200":
          description: Integration list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Integration"

  /settings/integrations/gmail/auth-url:
    get:
      operationId: getGmailAuthorizationUrl
      tags: [settings]
      summary: Start the authenticated Gmail OAuth flow
      responses:
        "200":
          description: Google authorization URL bound to an opaque server-side attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GmailAuthorizationUrl"

  /settings/integrations/gmail/finalize:
    post:
      operationId: finalizeGmailIntegration
      tags: [settings]
      summary: Finalize a Gmail OAuth callback with an authenticated session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GmailOAuthFinalizeInput"
      responses:
        "200":
          description: Connected Gmail integration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Integration"

  /settings/integrations/gmail/verify:
    post:
      operationId: verifyGmailMailbox
      tags: [settings]
      summary: Verify the Gmail connection and active inbound watch
      responses:
        "200":
          description: Gmail mailbox and inbound watch verified
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GmailMailboxVerification"

  /settings/integrations/gmail/disconnect:
    post:
      operationId: disconnectGmailIntegration
      tags: [settings]
      summary: Disconnect the authenticated workspace Gmail account
      responses:
        "200":
          description: Disconnected Gmail integration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Integration"

  /welcome/status:
    get:
      operationId: getWelcomeStatus
      tags: [welcome]
      summary: Get onboarding progress
      responses:
        "200":
          description: Welcome status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WelcomeStatus"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status: { type: string }
      required: [status]

    Recipient:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        email: { type: string }
        title: { type: string }
        company: { type: string }
        avatarUrl: { type: ["string", "null"] }
      required: [id, name, email, company]

    EvaluatorScores:
      type: object
      properties:
        pii: { type: number }
        hallucination: { type: number }
        citationCoverage: { type: number }
        toxicity: { type: number }
      required: [pii, hallucination, citationCoverage, toxicity]

    FactCitation:
      type: object
      properties:
        factId: { type: string }
        claim: { type: string }
        source: { type: string }
        date: { type: string }
        cited: { type: boolean }
      required: [factId, claim, source, cited]

    SendPolicy:
      type: object
      properties:
        liveSendEnabled: { type: boolean }
        postalAddressSet: { type: boolean }
        unsubscribeConfigured: { type: boolean }
        recipientSuppressed: { type: boolean }
      required:
        [
          liveSendEnabled,
          postalAddressSet,
          unsubscribeConfigured,
          recipientSuppressed,
        ]

    OutreachArtifact:
      type: object
      properties:
        id: { type: string }
        status:
          type: string
          enum:
            [
              DRAFT,
              PENDING_REVIEW,
              APPROVED,
              REJECTED,
              SENT,
              SUPPRESSED,
              SENDING,
              SIMULATED,
              DELIVERY_UNKNOWN,
              FAILED,
              RECONCILIATION_REQUIRED,
            ]
        purpose:
          type: string
          enum: [OUTBOUND, REPLY, FOLLOW_UP]
        channel:
          type: string
          enum: [EMAIL, LINKEDIN, HUBSPOT_NOTE, UNKNOWN]
        recipient:
          $ref: "#/components/schemas/Recipient"
        subject: { type: string }
        bodyText: { type: string }
        bodyHtml: { type: ["string", "null"] }
        citations:
          type: array
          items:
            $ref: "#/components/schemas/FactCitation"
        evaluatorScores:
          anyOf:
            - $ref: "#/components/schemas/EvaluatorScores"
            - type: "null"
        sendPolicy:
          anyOf:
            - $ref: "#/components/schemas/SendPolicy"
            - type: "null"
        refusal:
          $ref: "#/components/schemas/ArtifactRefusal"
        approvalEligibility:
          $ref: "#/components/schemas/ArtifactApprovalEligibility"
        langsmithRunId: { type: ["string", "null"] }
        createdAt: { type: string }
        updatedAt: { type: string }
        approvedAt: { type: ["string", "null"] }
        sentAt: { type: ["string", "null"] }
        rejectionReason: { type: ["string", "null"] }
        failureReason: { type: ["string", "null"] }
        failedAt: { type: ["string", "null"] }
        statusReason: { type: ["string", "null"] }
        sendReceiptId: { type: ["string", "null"] }
        graphRunId: { type: ["string", "null"] }
        cohort: { type: ["string", "null"] }
      required:
        [
          id,
          status,
          purpose,
          channel,
          recipient,
          subject,
          bodyText,
          bodyHtml,
          citations,
          evaluatorScores,
          sendPolicy,
          refusal,
          approvalEligibility,
          langsmithRunId,
          createdAt,
          updatedAt,
          approvedAt,
          sentAt,
          rejectionReason,
          failureReason,
          failedAt,
          statusReason,
          sendReceiptId,
          graphRunId,
          cohort,
        ]

    ArtifactRefusal:
      type: object
      properties:
        refused: { type: boolean }
        reason: { type: ["string", "null"] }
      required: [refused, reason]

    ArtifactApprovalEligibility:
      type: object
      properties:
        eligible: { type: boolean }
        reason: { type: ["string", "null"] }
      required: [eligible, reason]

    ArtifactApprovalFailure:
      type: object
      properties:
        message: { type: string }
        approvalSaved: { type: boolean }
        artifactId: { type: string }
        status:
          type: string
          enum: [APPROVED]
      required: [message, approvalSaved, artifactId]

    PaginatedArtifacts:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OutreachArtifact"
        total: { type: integer }
        page: { type: integer }
        limit: { type: integer }
      required: [items, total, page, limit]

    RejectInput:
      type: object
      properties:
        reason: { type: string }
      required: [reason]

    SuppressionRecordResult:
      type: object
      properties:
        id: { type: string }
        recipientRef: { type: string }
        reason:
          type: string
          enum: [USER_UNSUBSCRIBED, BOUNCED, COMPLAINED, MANUAL]
        source: { type: ["string", "null"] }
        created: { type: boolean }
        upgraded: { type: boolean }
      required: [id, recipientRef, reason, source, created, upgraded]

    ArtifactSuppressionResult:
      type: object
      properties:
        artifact:
          type: object
          properties:
            id: { type: string }
            status:
              type: string
              enum:
                [
                  DRAFT,
                  PENDING_REVIEW,
                  APPROVED,
                  REJECTED,
                  SENT,
                  SUPPRESSED,
                  SENDING,
                  SIMULATED,
                  DELIVERY_UNKNOWN,
                  FAILED,
                ]
            statusChanged: { type: boolean }
          required: [id, status, statusChanged]
        suppression:
          $ref: "#/components/schemas/SuppressionRecordResult"
      required: [artifact, suppression]

    BulkApproveResult:
      type: object
      properties:
        approved: { type: integer }
        skipped: { type: integer }
        reasons:
          type: array
          items: { type: string }
      required: [approved, skipped, reasons]

    ActivityEvent:
      type: object
      properties:
        id: { type: string }
        kind:
          type: string
          enum:
            [
              run_started,
              run_needs_approval,
              run_completed,
              run_failed,
              draft_created,
              draft_approved,
              draft_rejected,
              draft_failed,
              draft_sent,
              delivery_unknown,
              meeting_proposed,
              meeting_confirmed,
            ]
        action: { type: string }
        timestamp: { type: string }
        artifactId: { type: ["string", "null"] }
        leadId: { type: ["string", "null"] }
      required: [id, kind, action, timestamp, artifactId, leadId]

    TodayKpis:
      type: object
      properties:
        artifactsPending: { type: integer }
        artifactsSentToday: { type: integer }
        qualifiedMeetingsBooked: { type: integer }
        leadsScored: { type: integer }
      required:
        [
          artifactsPending,
          artifactsSentToday,
          qualifiedMeetingsBooked,
          leadsScored,
        ]

    IntentSignal:
      type: object
      properties:
        label: { type: string }
        confidence: { type: number }
      required: [label, confidence]

    Lead:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        title: { type: string }
        email: { type: string }
        company: { type: string }
        domain: { type: ["string", "null"] }
        companyLogoUrl: { type: ["string", "null"] }
        avatarUrl: { type: ["string", "null"] }
        score: { type: [integer, "null"] }
        stage: { type: string }
        geo: { type: ["string", "null"] }
        country: { type: ["string", "null"] }
        industry: { type: ["string", "null"] }
        headcountEstimate: { type: ["string", "null"] }
        cohort: { type: ["string", "null"], enum: [A, B, null] }
        emailStatus:
          {
            type: ["string", "null"],
            enum: [DELIVERABLE, HIGH_PROBABILITY, CATCH_ALL, null],
          }
        intentSignals:
          anyOf:
            - type: array
              items:
                $ref: "#/components/schemas/IntentSignal"
            - type: "null"
        lastContactedAt: { type: ["string", "null"] }
        sendPolicy:
          anyOf:
            - $ref: "#/components/schemas/SendPolicy"
            - type: "null"
        createdAt: { type: ["string", "null"] }
      required:
        [
          id,
          name,
          email,
          company,
          score,
          stage,
          cohort,
          emailStatus,
          intentSignals,
          sendPolicy,
          createdAt,
        ]

    ScoreBreakdown:
      type: object
      properties:
        fit: { type: integer }
        intent: { type: integer }
        engagement: { type: integer }
        timing: { type: integer }
      required: [fit, intent, engagement, timing]

    EvidenceEventSummary:
      type: object
      properties:
        id: { type: string }
        eventType: { type: string }
        description: { type: string }
        timestamp: { type: string }
        sourceUrl: { type: ["string", "null"], format: uri }
      required: [id, eventType, description, timestamp, sourceUrl]

    LeadDetail:
      type: object
      properties:
        lead:
          $ref: "#/components/schemas/Lead"
        researchBrief: { type: ["string", "null"] }
        scoreBreakdown:
          anyOf:
            - $ref: "#/components/schemas/ScoreBreakdown"
            - type: "null"
        recentEvidenceEvents:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceEventSummary"
      required: [lead, researchBrief, scoreBreakdown, recentEvidenceEvents]

    PaginatedLeads:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Lead"
        total: { type: integer }
        page: { type: integer }
        limit: { type: integer }
      required: [items, total, page, limit]

    TriggerResult:
      type: object
      properties:
        runId: { type: string }
        queued: { type: boolean }
        message: { type: string }
      required: [runId, queued, message]

    BulkLeadIds:
      type: object
      additionalProperties: false
      properties:
        personIds:
          type: array
          minItems: 1
          maxItems: 200
          items: { type: string, minLength: 1, maxLength: 256 }
      required: [personIds]

    PersonSuppressionResult:
      oneOf:
        - type: object
          properties:
            personId: { type: string }
            status: { type: string, enum: [SUPPRESSED] }
            suppression:
              $ref: "#/components/schemas/SuppressionRecordResult"
          required: [personId, status, suppression]
        - type: object
          properties:
            personId: { type: string }
            status: { type: string, enum: [SKIPPED] }
            reason:
              type: string
              enum: [NOT_FOUND_OR_CROSS_ORG, MISSING_EMAIL, AMBIGUOUS_EMAIL]
          required: [personId, status, reason]

    BulkPersonSuppressionResult:
      type: object
      properties:
        requestedCount: { type: integer }
        uniqueCount: { type: integer }
        affectedCount: { type: integer }
        alreadySuppressedCount: { type: integer }
        skippedCount: { type: integer }
        results:
          type: array
          items:
            $ref: "#/components/schemas/PersonSuppressionResult"
      required:
        [
          requestedCount,
          uniqueCount,
          affectedCount,
          alreadySuppressedCount,
          skippedCount,
          results,
        ]

    BulkActionResult:
      type: object
      properties:
        affected: { type: integer }
      required: [affected]

    CreateConversationReplyInput:
      type: object
      properties:
        subject:
          type: string
          minLength: 1
          maxLength: 200
          pattern: "^[^\\r\\n]*$"
        body:
          type: string
          minLength: 1
          maxLength: 8000
      required: [body]

    CreateConversationFollowUpInput:
      type: object
      properties:
        dueAt:
          type: string
          format: date-time
        note:
          type: string
          maxLength: 2000
      required: [dueAt]

    UpdateConversationFollowUpInput:
      type: object
      properties:
        status:
          type: string
          enum: [DONE, CANCELLED]
      required: [status]

    ConversationFollowUp:
      type: object
      properties:
        id: { type: string }
        conversationId: { type: string }
        dueAt: { type: string }
        note: { type: ["string", "null"] }
        status:
          type: string
          enum: [OPEN, DONE, CANCELLED]
        source:
          type: string
          enum: [HUMAN, AGENT]
        createdAt: { type: string }
        updatedAt: { type: string }
      required:
        [id, conversationId, dueAt, status, source, createdAt, updatedAt]

    CreateConversationMeetingInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        scheduledFor:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          minimum: 1
          maximum: 1440
        notes:
          type: string
          maxLength: 2000
      required: [scheduledFor]

    UpdateConversationMeetingInput:
      type: object
      minProperties: 1
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        scheduledFor:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          minimum: 1
          maximum: 1440
        notes:
          type: [string, "null"]
          maxLength: 2000

    CancelConversationMeetingInput:
      type: object
      properties:
        reason:
          type: string
          maxLength: 2000

    ConversationMeeting:
      type: object
      properties:
        id: { type: string }
        conversationId: { type: ["string", "null"] }
        title: { type: string }
        scheduledFor: { type: string }
        durationMinutes: { type: integer }
        attendeeEmails:
          type: array
          items: { type: string }
        notes: { type: ["string", "null"] }
        status:
          type: string
          enum: [PROPOSED, CONFIRMED, CANCELLED, COMPLETED, NO_SHOW]
        source:
          type: string
          enum: [AGENT_PROPOSED, HUMAN_LOGGED, IMPORTED]
        createdAt: { type: string }
        updatedAt: { type: string }
      required:
        [
          id,
          title,
          scheduledFor,
          durationMinutes,
          attendeeEmails,
          status,
          source,
          createdAt,
          updatedAt,
        ]

    ConversationMessage:
      type: object
      properties:
        id: { type: string }
        direction:
          type: string
          enum: [inbound, outbound]
        bodyHtml: { type: string }
        sentAt: { type: string }
        senderName: { type: string }
      required: [id, direction, bodyHtml, sentAt, senderName]

    ReplyIntelligence:
      type: object
      properties:
        analysisStatus:
          type: string
          enum: [PENDING, READY, FAILED]
          description: Whether the AI analysis is pending, usable, or failed.
        sentiment:
          type: [string, "null"]
          enum: [positive, objection, neutral, negative, null]
        sentimentConfidence: { type: [number, "null"] }
        nextBestAction: { type: [string, "null"] }
        nextBestActionType:
          type: [string, "null"]
          enum: [send_content, qualify, disqualify, follow_up, null]
      required:
        [
          analysisStatus,
          sentiment,
          sentimentConfidence,
          nextBestAction,
          nextBestActionType,
        ]

    Conversation:
      type: object
      properties:
        id: { type: string }
        leadId: { type: ["string", "null"] }
        leadName: { type: string }
        leadCompany: { type: string }
        leadAvatarUrl: { type: ["string", "null"] }
        subject: { type: string }
        lastMessagePreview: { type: string }
        lastMessageAt: { type: string }
        unread: { type: boolean }
        needsReply: { type: boolean }
        archived: { type: boolean }
        replyIntelligence:
          $ref: "#/components/schemas/ReplyIntelligence"
      required:
        [
          id,
          leadName,
          leadCompany,
          subject,
          lastMessagePreview,
          lastMessageAt,
          unread,
          needsReply,
          archived,
          replyIntelligence,
        ]

    ConversationDetail:
      type: object
      properties:
        conversation:
          $ref: "#/components/schemas/Conversation"
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ConversationMessage"
        pendingDraftId: { type: ["string", "null"] }
        followUps:
          type: array
          items:
            $ref: "#/components/schemas/ConversationFollowUp"
        meetings:
          type: array
          items:
            $ref: "#/components/schemas/ConversationMeeting"
      required: [conversation, messages, followUps, meetings]

    PaginatedConversations:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Conversation"
        total: { type: integer }
        page: { type: integer }
        limit: { type: integer }
      required: [items, total, page, limit]

    SendReadiness:
      type: object
      properties:
        liveSendAllowed: { type: boolean }
        physicalAddressSet: { type: boolean }
        senderNameSet: { type: boolean }
        countrySet: { type: boolean }
        mailboxConnected: { type: boolean }
        dailyCapRemaining: { type: [integer, "null"] }
      required:
        [
          liveSendAllowed,
          physicalAddressSet,
          senderNameSet,
          countrySet,
          mailboxConnected,
          dailyCapRemaining,
        ]

    OrgSettings:
      type: object
      properties:
        orgId: { type: string }
        orgName: { type: string }
        slug: { type: string }
        website: { type: ["string", "null"] }
        logoUrl: { type: ["string", "null"] }
        country: { type: string }
        timezone: { type: string }
        senderName: { type: ["string", "null"] }
        liveSendEnabled: { type: boolean }
        postalAddress: { type: ["string", "null"] }
        unsubscribeUrl: { type: ["string", "null"] }
        suppressionCount: { type: [integer, "null"] }
        allowlistedDomains:
          type: array
          items: { type: string }
        plan: { type: [string, "null"] }
        creditsRemaining: { type: [integer, "null"] }
        welcomeComplete: { type: boolean }
        canReviewArtifacts: { type: [boolean, "null"] }
        canManageWorkflow: { type: [boolean, "null"] }
        canManageMailbox: { type: [boolean, "null"] }
        canManageOrg: { type: [boolean, "null"] }
        canManageSuppressions: { type: [boolean, "null"] }
        sendReadiness:
          anyOf:
            - $ref: "#/components/schemas/SendReadiness"
            - type: "null"
      required:
        [
          orgId,
          orgName,
          slug,
          website,
          country,
          timezone,
          liveSendEnabled,
          suppressionCount,
          allowlistedDomains,
          plan,
          creditsRemaining,
          welcomeComplete,
          canReviewArtifacts,
          canManageWorkflow,
          canManageMailbox,
          canManageOrg,
          canManageSuppressions,
          sendReadiness,
        ]

    UpdateOrgInput:
      type: object
      properties:
        name: { type: string }
        website: { type: string }
        slug: { type: string }
        logoUrl: { type: string }
        country: { type: string }
        timezone: { type: string }
        senderName: { type: string }
        postalAddress: { type: string }
        liveSendEnabled: { type: boolean }

    OrgHealth:
      type: object
      properties:
        liveSendEnabled: { type: boolean }
        postalAddressConfigured: { type: boolean }
        unsubscribeConfigured: { type: boolean }
        suppressionCount: { type: integer }
        blockers:
          type: array
          items: { type: string }
      required:
        [
          liveSendEnabled,
          postalAddressConfigured,
          unsubscribeConfigured,
          suppressionCount,
          blockers,
        ]

    SuppressionReason:
      type: string
      enum: [USER_UNSUBSCRIBED, BOUNCED, COMPLAINED, MANUAL]

    SuppressionEntry:
      type: object
      properties:
        id: { type: string }
        recipientRef: { type: string }
        reason:
          $ref: "#/components/schemas/SuppressionReason"
        source: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
      required: [id, recipientRef, reason, source, createdAt]

    SuppressionPage:
      type: object
      properties:
        rows:
          type: array
          items:
            $ref: "#/components/schemas/SuppressionEntry"
        nextCursor: { type: [string, "null"] }
      required: [rows, nextCursor]

    CreateSuppressionInput:
      type: object
      additionalProperties: false
      properties:
        recipientRef: { type: string, minLength: 1, maxLength: 512 }
        reason:
          type: string
          enum: [MANUAL, COMPLAINED]
          default: MANUAL
      required: [recipientRef]

    CreateSuppressionResult:
      type: object
      properties:
        created: { type: boolean }
        recipientRef: { type: string }
        reason:
          type: string
          enum: [MANUAL, COMPLAINED]
      required: [created, recipientRef, reason]

    IcpProfile:
      type: object
      properties:
        industries:
          type: array
          items: { type: string }
        titles:
          type: array
          items: { type: string }
        geos:
          type: array
          items: { type: string }
        sizeBand: { type: string }
        techStackSignals:
          type: array
          items: { type: string }
        intentSignals:
          type: array
          items: { type: string }
        seedDomains:
          type: array
          items: { type: string }
        exclusionDomains:
          type: array
          items: { type: string }
      required:
        [
          industries,
          titles,
          geos,
          sizeBand,
          techStackSignals,
          intentSignals,
          seedDomains,
          exclusionDomains,
        ]

    Integration:
      type: object
      properties:
        id: { type: string }
        provider: { type: string }
        status:
          type: string
          enum: [connected, available, errored]
        accountEmail: { type: ["string", "null"] }
        connectedAt: { type: ["string", "null"] }
        errorMessage: { type: ["string", "null"] }
      required: [id, provider, status]

    GmailOAuthFinalizeInput:
      type: object
      properties:
        attemptId: { type: string, minLength: 1 }
      required: [attemptId]

    GmailAuthorizationUrl:
      type: object
      properties:
        authUrl: { type: string, format: uri }
      required: [authUrl]

    GmailMailboxVerification:
      type: object
      properties:
        verified: { type: boolean, enum: [true] }
        watchExpiresAt: { type: string, format: date-time }
      required: [verified, watchExpiresAt]

    WelcomeStatus:
      type: object
      properties:
        organization:
          type: object
          properties:
            nameSet: { type: boolean }
            websiteSet: { type: boolean }
            complete: { type: boolean }
          required: [nameSet, websiteSet, complete]
        senderIdentity:
          type: object
          properties:
            senderNameSet: { type: boolean }
            countrySet: { type: boolean }
            physicalAddressSet: { type: boolean }
            complete: { type: boolean }
          required: [senderNameSet, countrySet, physicalAddressSet, complete]
        icp:
          type: object
          properties:
            usable: { type: boolean }
            complete: { type: boolean }
          required: [usable, complete]
        mailbox:
          type: object
          properties:
            connected: { type: boolean }
            complete: { type: boolean }
          required: [connected, complete]
        sendReadiness:
          $ref: "#/components/schemas/SendReadiness"
        complete: { type: boolean }
        currentStep:
          type: string
          enum: [organization, sender_identity, icp, mailbox, complete]
        readyForLiveSend: { type: boolean }
      required:
        [
          organization,
          senderIdentity,
          icp,
          mailbox,
          sendReadiness,
          complete,
          currentStep,
          readyForLiveSend,
        ]

    GraphRun:
      type: object
      properties:
        id: { type: string }
        status:
          type: string
          enum: [RUNNING, AWAITING_APPROVAL, COMPLETED, FAILED, CANCELLED]
        stagesCompleted:
          type: array
          items: { type: string }
        leadsScored: { type: [integer, "null"] }
        artifactsGenerated: { type: [integer, "null"] }
        durationMs: { type: integer }
        costUsd: { type: [number, "null"] }
        approvedBy: { type: ["string", "null"] }
        startedAt: { type: string }
        completedAt: { type: ["string", "null"] }
      required:
        [
          id,
          status,
          stagesCompleted,
          leadsScored,
          artifactsGenerated,
          durationMs,
          costUsd,
          approvedBy,
          startedAt,
          completedAt,
        ]

    PaginatedRuns:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GraphRun"
        total: { type: integer }
        page: { type: integer }
        limit: { type: integer }
      required: [items, total, page, limit]

    GraphRunDetail:
      type: object
      properties:
        run:
          $ref: "#/components/schemas/GraphRun"
        timeline:
          type: array
          items:
            $ref: "#/components/schemas/TimelineNode"
      required: [run, timeline]

    TimelineNode:
      type: object
      properties:
        id: { type: string }
        nodeType:
          type: string
          enum: [agent_run, llm_call, evaluator, tool_call, human_action]
        label: { type: string }
        summary: { type: string }
        reasoning: { type: ["string", "null"] }
        tokensUsed: { type: ["integer", "null"] }
        durationMs: { type: ["integer", "null"] }
        cost: { type: ["number", "null"] }
        score: { type: ["number", "null"] }
        timestamp: { type: string }
        children:
          type: array
          items:
            $ref: "#/components/schemas/TimelineNode"
      required: [id, nodeType, label, summary, timestamp, children]
